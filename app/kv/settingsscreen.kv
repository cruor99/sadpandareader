
<SettingsScreen>:
    BoxLayout:
        orientation: "vertical"
        Toolbar:
            id: toolbar
            title: root.title
            #left_action_items: [["menu", lambda x: app.nav_drawer.toggle(), False]]
            left_action_items: [["chevron-left", lambda x: app.root.goto_front(), False]]
            right_action_items: [["menu", lambda x: app.nav_drawer.toggle(), False]]
            background_color: app.theme_cls.primary_color
        RecycleView:
            MDList:
                id: settingslist
                OneLineListItem:
                    text: "Filters"
                    on_release: app.root.show_filters()
